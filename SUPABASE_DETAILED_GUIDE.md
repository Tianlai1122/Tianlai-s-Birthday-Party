# 🚀 Supabase 超详细部署教程（一步一步）

## 📌 目录

1. [第一部分：创建 Supabase 账户](#第一部分创建-supabase-账户)
2. [第二部分：创建项目](#第二部分创建项目)
3. [第三部分：创建数据表](#第三部分创建数据表)
4. [第四部分：配置 Render](#第四部分配置-render)
5. [第五部分：推送代码](#第五部分推送代码)
6. [第六部分：验证部署](#第六部分验证部署)
7. [第七部分：测试功能](#第七部分测试功能)

---

## 第一部分：创建 Supabase 账户

### 步骤 1.1：打开 Supabase 官网

1. 打开浏览器
2. 访问：https://supabase.com
3. 你会看到这样的页面：

```
┌─────────────────────────────────────┐
│  Supabase - The Open Source         │
│  Firebase Alternative               │
│                                     │
│  [Sign Up]  [Sign In]               │
└─────────────────────────────────────┘
```

### 步骤 1.2：点击 Sign Up

1. 点击页面右上角的 **"Sign Up"** 按钮
2. 你会看到登录选项

### 步骤 1.3：用 GitHub 账户注册

1. 点击 **"Continue with GitHub"**
2. 如果你还没登录 GitHub，会跳转到 GitHub 登录页面
3. 输入你的 GitHub 用户名和密码
4. 点击 **"Sign in"**

### 步骤 1.4：授权 Supabase

1. GitHub 会要求授权，点击 **"Authorize supabase"**
2. 等待几秒钟
3. 你会被重定向回 Supabase
4. 完成！你现在已经登录了

---

## 第二部分：创建项目

### 步骤 2.1：进入 Dashboard

登录后，你会看到 Supabase Dashboard：

```
┌──────────────────────────────────────────┐
│  Welcome to Supabase!                    │
│                                          │
│  [New Project]  [Import]                 │
│                                          │
│  Your Projects:                          │
│  (空的，因为这是第一次)                   │
└──────────────────────────────────────────┘
```

### 步骤 2.2：点击 New Project

1. 点击 **"New Project"** 按钮
2. 你会看到项目创建表单

### 步骤 2.3：填写项目信息

你会看到这样的表单：

```
┌─────────────────────────────────────────┐
│  Create a new project                   │
│                                         │
│  Name: [________________]                │
│  Database Password: [________________]   │
│  Region: [Southeast Asia (Singapore)]   │
│  Pricing Plan: [Free]                   │
│                                         │
│  [Create new project]                   │
└─────────────────────────────────────────┘
```

**填写方法：**

**1. Name 字段：**
- 输入：`tianlai-birthday-party`
- 这是你的项目名称，可以随意修改

**2. Database Password 字段：**
- 输入一个强密码，例如：`MySecurePassword123!@#`
- ⚠️ **重要：记住这个密码！** 虽然我们不会直接用到，但以后可能需要
- 建议使用：大小写字母 + 数字 + 特殊符号

**3. Region 下拉菜单：**
- 点击下拉菜单
- 选择 **"Southeast Asia (Singapore)"** 或 **"Northeast Asia (Tokyo)"**
- 这两个离中国最近，速度最快
- 如果你在美国，选择 **"US East (N. Virginia)"**

**4. Pricing Plan：**
- 应该已经选中 **"Free"**
- 这是免费版本，完全够用

### 步骤 2.4：创建项目

1. 检查所有信息是否正确
2. 点击 **"Create new project"** 按钮
3. 等待 1-2 分钟，Supabase 会创建你的项目

你会看到这样的加载界面：

```
┌──────────────────────────────────────┐
│  Setting up your project...          │
│                                      │
│  ⏳ Creating database                │
│  ⏳ Setting up authentication        │
│  ⏳ Initializing storage             │
│                                      │
│  This may take a minute...           │
└──────────────────────────────────────┘
```

### 步骤 2.5：项目创建完成

当看到这样的界面，说明项目创建成功了：

```
┌──────────────────────────────────────┐
│  ✅ Project Created!                 │
│                                      │
│  Project Name: tianlai-birthday-party│
│  Region: Southeast Asia (Singapore)  │
│  Status: Active                      │
│                                      │
│  [Go to Dashboard]                   │
└──────────────────────────────────────┘
```

点击 **"Go to Dashboard"** 进入项目

---

## 第三部分：创建数据表

### 步骤 3.1：进入 Table Editor

在 Supabase Dashboard 中：

1. 左侧菜单找到 **"SQL Editor"** 或 **"Table Editor"**
2. 点击 **"Table Editor"**

你会看到：

```
┌──────────────────────────────────────┐
│  Table Editor                        │
│                                      │
│  [Create a new table]                │
│                                      │
│  Tables:                             │
│  (空的)                              │
└──────────────────────────────────────┘
```

### 步骤 3.2：创建新表

1. 点击 **"Create a new table"** 按钮
2. 你会看到表创建对话框

### 步骤 3.3：填写表信息

对话框会显示：

```
┌─────────────────────────────────────┐
│  Create a new table                 │
│                                     │
│  Name: [________________]            │
│  Description: [________________]     │
│                                     │
│  ☑ Enable Row Level Security (RLS) │
│                                     │
│  [Create table]                     │
└─────────────────────────────────────┘
```

**填写方法：**

**1. Name 字段：**
- 输入：`party_data`
- 这是表的名称，必须是这个名字

**2. Description 字段：**
- 输入：`Birthday party data storage`
- 这是可选的，用来描述表的用途

**3. Row Level Security (RLS)：**
- ⚠️ **重要：取消勾选这个选项！**
- 点击复选框，使其变为未勾选状态
- 这样可以简化配置

### 步骤 3.4：点击创建

1. 检查信息无误
2. 点击 **"Create table"** 按钮
3. 表会被创建

### 步骤 3.5：添加列（Columns）

表创建后，你会看到：

```
┌──────────────────────────────────────┐
│  party_data                          │
│                                      │
│  Columns:                            │
│  • id (uuid) [Primary Key]           │
│                                      │
│  [Add column]                        │
└──────────────────────────────────────┘
```

Supabase 默认创建了一个 `id` 列。我们需要修改它并添加其他列。

**删除默认的 id 列：**

1. 点击 `id` 列右边的三个点 **"..."**
2. 选择 **"Delete column"**
3. 确认删除

**添加新的 id 列（文本类型）：**

1. 点击 **"Add column"** 按钮
2. 填写：
   - **Name**: `id`
   - **Type**: 选择 **"text"**
   - **Primary Key**: 勾选 ✅
   - **Nullable**: 不勾选 ❌
3. 点击 **"Save"**

**添加 data 列：**

1. 点击 **"Add column"** 按钮
2. 填写：
   - **Name**: `data`
   - **Type**: 选择 **"jsonb"**（JSON Binary）
   - **Primary Key**: 不勾选 ❌
   - **Nullable**: 不勾选 ❌
3. 点击 **"Save"**

**添加 updated_at 列：**

1. 点击 **"Add column"** 按钮
2. 填写：
   - **Name**: `updated_at`
   - **Type**: 选择 **"timestamptz"**
   - **Default Value**: 点击 **"now()"**
   - **Primary Key**: 不勾选 ❌
   - **Nullable**: 不勾选 ❌
3. 点击 **"Save"**

完成后，你的表应该看起来像这样：

```
┌────────────────────────────────────────┐
│  party_data                            │
│                                        │
│  Columns:                              │
│  • id (text) [Primary Key]             │
│  • data (jsonb)                        │
│  • updated_at (timestamptz)            │
│                                        │
│  [Add column]                          │
└────────────────────────────────────────┘
```

### 步骤 3.6：插入初始数据

1. 在表编辑器中，点击 **"Insert row"** 按钮
2. 你会看到一个新行被添加

3. 点击 `id` 字段，输入：`main`

4. 点击 `data` 字段，你会看到一个 JSON 编辑器

5. 在 JSON 编辑器中，粘贴以下内容：

```json
{
  "foodies": [],
  "drinkers": [],
  "gamePreferences": [],
  "vibeVotes": [],
  "krystalLikes": 0,
  "memberLikes": {},
  "memberComments": {},
  "customMembers": [],
  "gameLobbies": [],
  "visits": 0,
  "lastVisit": null,
  "visitHistory": []
}
```

6. 点击 **"Save"** 或按 **Ctrl+S**

7. `updated_at` 字段会自动填充当前时间

完成！你的数据表现在看起来像这样：

```
┌──────────────────────────────────────────┐
│  party_data                              │
│                                          │
│  id    | data                | updated_at│
│  main  | {...}               | 2024-...  │
│                                          │
└──────────────────────────────────────────┘
```

---

## 第四部分：获取 API 凭证

### 步骤 4.1：进入 Settings

1. 在 Supabase Dashboard 左侧菜单
2. 找到 **"Settings"**（齿轮图标）
3. 点击进入

### 步骤 4.2：找到 API 部分

1. 在 Settings 页面，找到 **"API"** 选项卡
2. 点击进入

你会看到：

```
┌──────────────────────────────────────────┐
│  API Settings                            │
│                                          │
│  Project URL:                            │
│  https://xxxxxxxxxxxxx.supabase.co       │
│                                          │
│  API Keys:                               │
│  • anon (public)                         │
│  • service_role (secret)                 │
│                                          │
└──────────────────────────────────────────┘
```

### 步骤 4.3：复制 Project URL

1. 找到 **"Project URL"** 字段
2. 点击右边的复制按钮 📋
3. 粘贴到记事本，保存下来

例如：
```
https://abcdefghijklmnop.supabase.co
```

### 步骤 4.4：复制 API Key

1. 找到 **"anon (public)"** 的 API Key
2. 点击右边的复制按钮 📋
3. 粘贴到记事本，保存下来

例如：
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTcwMDAwMDAwMCwiZXhwIjoxODAwMDAwMDAwfQ.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

⚠️ **重要：保存这两个值！接下来会用到！**

---

## 第五部分：配置 Render

### 步骤 5.1：登录 Render

1. 打开浏览器
2. 访问：https://dashboard.render.com
3. 用你的账户登录

### 步骤 5.2：找到你的后端服务

在 Render Dashboard 中：

1. 你会看到你的服务列表
2. 找到名字类似 **"tianlai-backend"** 或 **"tianlai-s-birthday-party"** 的服务
3. 点击进入

### 步骤 5.3：进入 Environment 设置

1. 在服务页面，左侧菜单找到 **"Environment"**
2. 点击进入

你会看到：

```
┌──────────────────────────────────────┐
│  Environment                         │
│                                      │
│  Environment Variables:              │
│  (可能是空的，或有其他变量)           │
│                                      │
│  [Add Environment Variable]          │
└──────────────────────────────────────┘
```

### 步骤 5.4：添加第一个环境变量

1. 点击 **"Add Environment Variable"** 按钮
2. 你会看到一个表单：

```
┌──────────────────────────────────────┐
│  Add Environment Variable            │
│                                      │
│  Key: [________________]              │
│  Value: [________________]            │
│                                      │
│  [Add]  [Cancel]                     │
└──────────────────────────────────────┘
```

3. 填写：
   - **Key**: `SUPABASE_URL`
   - **Value**: 粘贴你之前复制的 Project URL
   
   例如：
   ```
   https://abcdefghijklmnop.supabase.co
   ```

4. 点击 **"Add"** 按钮

### 步骤 5.5：添加第二个环境变量

1. 再次点击 **"Add Environment Variable"** 按钮
2. 填写：
   - **Key**: `SUPABASE_KEY`
   - **Value**: 粘贴你之前复制的 API Key
   
   例如：
   ```
   eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSI...
   ```

3. 点击 **"Add"** 按钮

### 步骤 5.6：保存更改

1. 页面会自动保存
2. 你会看到一条消息：**"Environment variables updated"**
3. Render 会自动重新部署你的服务

等待 2-3 分钟，让 Render 重新部署。

---

## 第六部分：推送代码

### 步骤 6.1：打开终端

1. 打开你的终端/命令行
2. 进入项目目录：

```bash
cd /Users/tianlaizhang/Downloads/LOL
```

### 步骤 6.2：安装依赖

```bash
cd backend
npm install
```

这会安装 `@supabase/supabase-js` 包。

### 步骤 6.3：返回项目根目录

```bash
cd ..
```

### 步骤 6.4：提交代码

```bash
git add .
git commit -m "迁移到 Supabase 数据库"
```

### 步骤 6.5：推送到 GitHub

```bash
git push
```

等待推送完成。

---

## 第七部分：验证部署

### 步骤 7.1：检查 Render 日志

1. 回到 Render Dashboard
2. 点击你的后端服务
3. 点击 **"Logs"** 标签
4. 等待 2-3 分钟，看新的日志

你应该看到类似这样的日志：

```
2024-01-15 10:30:45 npm install
2024-01-15 10:31:20 npm start
2024-01-15 10:31:25 ✅ Supabase 连接成功！数据将持久化保存
2024-01-15 10:31:26 ✅ 从 Supabase 加载数据成功
2024-01-15 10:31:27 🎉 生日派对服务器已启动！
2024-01-15 10:31:27 API 服务: http://localhost:3000
2024-01-15 10:31:27 数据存储: Supabase (持久化)
```

✅ **如果看到这些日志，说明连接成功了！**

### 步骤 7.2：检查错误日志

如果看到错误，常见的有：

**错误 1：连接失败**
```
❌ Supabase 连接失败，降级使用文件系统
```

**解决方法：**
- 检查 Render 环境变量是否正确
- 检查 SUPABASE_URL 和 SUPABASE_KEY 是否完整复制
- 确保没有多余的空格

**错误 2：表不存在**
```
Error: relation "party_data" does not exist
```

**解决方法：**
- 检查 Supabase 中表名是否为 `party_data`
- 检查表是否已创建

---

## 第八部分：测试功能

### 步骤 8.1：访问你的网站

1. 打开浏览器
2. 访问你的前端 URL：
   ```
   https://tianlai-s-birthday-party.vercel.app
   ```

### 步骤 8.2：测试功能

1. **点赞功能**：点击 😍 按钮
2. **发送留言**：在留言框输入内容并提交
3. **添加小馋猫**：输入名字并添加
4. **创建游戏组局**：创建一个新的游戏组局

### 步骤 8.3：验证数据保存

1. 回到 Supabase Dashboard
2. 点击 **"Table Editor"**
3. 选择 `party_data` 表
4. 点击 `id = main` 的行
5. 查看 `data` 字段

你应该能看到你刚才添加的数据！

例如：
```json
{
  "foodies": ["张三", "李四"],
  "drinkers": [],
  "gamePreferences": [],
  "vibeVotes": [],
  "krystalLikes": 5,
  "memberLikes": {...},
  "memberComments": {...},
  ...
}
```

✅ **如果能看到数据，说明一切正常！**

---

## 🎉 完成！

你已经成功：
- ✅ 创建了 Supabase 项目
- ✅ 创建了数据表
- ✅ 配置了 Render 环境变量
- ✅ 推送了代码
- ✅ 验证了部署
- ✅ 测试了功能

你的数据现在将永久保存在 Supabase 中！

---

## 🆘 常见问题

### Q: 我忘记了 API Key 怎么办？

A: 回到 Supabase Dashboard → Settings → API，重新复制即可。

### Q: 数据没有保存怎么办？

A: 
1. 检查 Render 日志是否显示连接成功
2. 检查 Supabase 表中是否有数据
3. 检查浏览器控制台是否有错误

### Q: 如何修改数据？

A: 在 Supabase Dashboard → Table Editor 中，直接编辑 `data` 字段即可。

### Q: 如何备份数据？

A: 在 Supabase Dashboard → Table Editor 中，选择表，点击导出按钮。

---

## 📞 需要帮助？

如果遇到问题，请检查：
1. Supabase 项目是否创建成功
2. 表名是否为 `party_data`
3. RLS 是否已禁用
4. Render 环境变量是否正确
5. 代码是否已推送
6. Render 是否已重新部署

祝你部署顺利！🚀

